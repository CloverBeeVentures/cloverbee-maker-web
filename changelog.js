const buttons=[...document.querySelectorAll('.filter-button')];const releases=[...document.querySelectorAll('.release-card')];const empty=document.querySelector('.empty-state');buttons.forEach(button=>{button.addEventListener('click',()=>{const selected=button.dataset.platform;buttons.forEach(item=>{const active=item===button;item.classList.toggle('active',active);item.setAttribute('aria-pressed',String(active));});let visible=0;releases.forEach(release=>{const show=selected==='all'||release.dataset.platform===selected;release.hidden=!show;if(show)visible+=1;});if(empty)empty.hidden=visible!==0;});});